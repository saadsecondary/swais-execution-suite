/**
 * Newsletter popup has been removed from the site. The only subscribe entry
 * point is the checkbox inside the contact form. This component is kept as a
 * no-op so any lingering import does not break the build.
 */
const NewsletterPopup = () => null;

export default NewsletterPopup;
