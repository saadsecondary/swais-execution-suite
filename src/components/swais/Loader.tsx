/**
 * Loader has been disabled. The hero must appear instantly with no overlay,
 * flash, or buffering UI. This component is intentionally a no-op so existing
 * imports keep working.
 */
const Loader = () => null;

export default Loader;
